schemaVersion: 2.2.0
metadata:
  name: cpp-course-multi-exercise
attributes:
  editor:
    id: eclipse/che-theia/latest
    plugins:
      - ms-vscode.cpptools
  plugin-registry-url: https://open-vsx.org

components:
  - name: tools
    container:
      image: registry.redhat.io/devspaces/udi-base-rhel9
      memoryLimit: 512Mi
      mountSources: true
      command:
        - bash
      args:
        - -c
        - |
          while sleep 1000; do :; done
